// const PageTitle = (props) => {
//   return (
//     <h2 className="shops-section-title">{props.title}</h2>
//   );
// }

const PageTitle = ({title}) => {
  return (
    <h2 className="shops-section-title">{title}</h2>
  );
}

export default PageTitle;